{
  "name": "@redestiny/nibot",
  "version": "0.2.0",
  "description": "Local CLI MVP for AI-assisted long-form writing workflows.",
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "nibot": "./dist/src/cli/index.js"
  },
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run build -w web",
    "dev": "tsx src/cli/index.ts",
    "dev:web": "npm run dev -w web",
    "test": "vitest run",
    "test:web": "npm run test -w web"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.86.1",
    "@hono/node-server": "^1.14.0",
    "commander": "^14.0.3",
    "diff": "^8.0.4",
    "hono": "^4.7.0",
    "openai": "^6.34.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
